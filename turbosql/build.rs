// this file must exist,
// so that the OUT_DIR env var is set on build,
// so that we can find the migrations.toml file,
// so that we can embed it in your binary,
// so that we can run your migrations.

fn main() {}
