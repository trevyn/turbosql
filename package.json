{
 "scripts": {
  "doc": "cargo doc --no-deps",
  "test": "cargo test --manifest-path=turbosql/Cargo.toml --features test -- --test-threads=1 && cargo test --manifest-path=turbosql-impl/Cargo.toml --features test"
 }
}
